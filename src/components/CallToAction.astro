<section class="cta section container" aria-labelledby="cta-title">
  <div class="cta__inner">
    <p class="eyebrow">El próximo proyecto empieza aquí</p>
    <h2 id="cta-title">Hablemos de la infraestructura que necesita construir.</h2>
    <p>Presentemos juntos una propuesta sólida, precisa y lista para avanzar.</p>
    <a class="button" href="/contacto/">Presentar un proyecto <span aria-hidden="true">↗</span></a>
  </div>
</section>

<style>
  .cta__inner {
    max-width: 54rem;
    padding: clamp(2.5rem, 7vw, 6rem);
    background: var(--color-primary);
    color: var(--color-bg);
  }

  .eyebrow {
    margin: 0 0 var(--space-6);
    color: var(--color-accent-soft);
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  h2 {
    max-width: 15ch;
    margin: 0;
    font-size: var(--text-2xl);
    letter-spacing: -0.05em;
    line-height: var(--leading-tight);
  }

  .cta__inner > p:not(.eyebrow) {
    max-width: 30rem;
    margin: var(--space-6) 0 0;
    color: rgb(246 245 241 / 72%);
  }

  .button {
    display: inline-flex;
    gap: var(--space-4);
    align-items: center;
    margin-top: var(--space-8);
    padding: 0.9rem 1.25rem;
    background: var(--color-accent);
    color: var(--color-primary-strong);
    font-size: var(--text-sm);
    font-weight: 700;
    text-decoration: none;
  }

  .button:hover {
    background: #d9aa4c;
  }
</style>
